{
  "name": "protractor",
  "version": "1.0.0",
  "description": "demo for protractor",
  "main": "conf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jasmine-allure-reporter": "1.0.2",
    "protractor-html-reporter-2": "^1.0.4"
  },
  "devDependencies": {
    "faker": "^4.1.0",
    "protractor-jasmine2-screenshot-reporter": "^0.5.0"
  }
}
